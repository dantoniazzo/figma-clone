import { Toolbar, Canvas } from 'widgets';

function App() {
  return (
    <div className="h-screen w-screen bg-black touch-none">
      <Canvas />
      <Toolbar />
    </div>
  );
}

export default App;
